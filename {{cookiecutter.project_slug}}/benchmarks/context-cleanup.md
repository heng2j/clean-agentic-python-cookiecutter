# Benchmark: context cleanup

Assume someone appended “Always read every Markdown file before editing” to root agent guidance. Remove the low-precision instruction without moving it elsewhere. Run context/docs audits and explain the authority change.
