# Benchmark: bounded local change

Add evidence age as an explicit release-blocking policy. Keep clock acquisition outside the pure domain, update the contract and tests, add one curated mutant, avoid the harness, and return exact evidence plus rollback.
